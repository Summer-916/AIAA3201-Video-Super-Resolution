from run_part1 import main


if __name__ == "__main__":
    # Historical convenience entry point: running this file is equivalent to
    # running scripts/run_part1.py directly. It is kept so older commands in
    # notebooks or shell history still work.
    main()
